README

TODO: try playing with erlport/pyro

http://erlport.org/docs/python.html

https://pythonhosted.org/Pyro4/intro.html


- To start nameserver run:

pyro4-ns -n "hostname"

Davids works.


awk 'BEGIN { FS="" } FNR==NR { if (length > max) max = length; next } { while (length < max) $0=$0 OFS; for (i=NF; i>=1; i--) printf (i!=1) ? $i : $i ORS }' <(echo "hello" | cowsay){,}